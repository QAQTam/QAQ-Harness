# Handover 2026-09-09：系统重置前快照与恢复指南

> 本文 + `.workbuddy/memory/`（已入库）是系统重置后恢复工作状态的**两个入口**：
> 本文管「资产在哪、怎么恢复」，记忆目录管「项目怎么继续做」。

## 1. 背景

2026-09-09 重置整个系统。重置前已完成全量上云（本日全部工作已合并推送）。
本文记录云端资产清单、恢复顺序与未竟事项。

## 2. 云端资产清单（github.com/QAQTam，均 public）

| 仓库 | 内容 | 重置前最新 |
|---|---|---|
| [QAQ-Harness](https://github.com/QAQTam/QAQ-Harness) | 主仓：daemon + 15 crates + 契约文档 | `54f391b`（main） |
| [qaqh-js-sdk](https://github.com/QAQTam/qaqh-js-sdk) | Ringing V1 TypeScript SDK | `205c8f2`（main，初始导入） |
| [qaqh-electron](https://github.com/QAQTam/qaqh-electron) | Electron 44 + SolidJS 2 桌面壳 | `fcff0e5`（main，初始导入） |
| [qaqh-tui-app](https://github.com/QAQTam/qaqh-tui-app) | Rust TUI 壳 | `f85c33d`（子代理实时观测） |
| secret gist `81fc8df2` | WorkBuddy 状态：身份文件、两级记忆、mcp.json、dev-env.json、skills 清单 | https://gist.github.com/QAQTam/81fc8df2ce38c9c78db4da96d7a8f598 |

远端分支 `origin/refactor/tool-result-projection` 保留中，与 main 同指向（`54f391b`），可删。

## 3. 恢复 checklist（按序）

1. **clone 主仓**（含 `.workbuddy/memory/`——WorkBuddy 项目记忆随仓库走，clone 即恢复）：
   ```bash
   git clone https://github.com/QAQTam/QAQ-Harness && cd QAQ-Harness
   ```
2. **clone 三个壳/SDK 仓**（electron 与 js-sdk 同级放置：electron 通过 `file:../qaqh-js-sdk` 引用 SDK）：
   ```bash
   git clone https://github.com/QAQTam/qaqh-js-sdk ~/Projects/qaqh-js-sdk
   git clone https://github.com/QAQTam/qaqh-electron ~/Projects/qaqh-electron
   git clone https://github.com/QAQTam/qaqh-tui-app ~/Projects/qaqh-tui-app
   ```
3. **恢复 WorkBuddy 状态**：从 gist `81fc8df2` 取回——
   - `user-MEMORY.md` → `~/.workbuddy/MEMORY.md`（跨项目偏好）
   - `SOUL.md` / `IDENTITY.md` / `USER.md` / `BOOTSTRAP.md` → `~/.workbuddy/`
   - `mcp.json` → `~/.workbuddy/mcp.json`（注意：**确认连接器后需在连接器管理页对自定义 server 点信任才生效**）
   - `dev-env.json` → `~/.workbuddy/dev-env.json`
   - skills：按 `skills-manifest.txt` 重装（`npx skills` 装 `~/.agents/skills/`，再软链进 `~/.workbuddy/skills/`）
   - 恢复完可删除 gist。
4. **验证主仓**：`cargo check --workspace --all-targets` 干净；`cargo test -p qaqh-workspace --test todo_contract` 应**通过**（9-09 已修复历史失败）。

## 4. 各仓状态与未竟事项

### QAQ-Harness（main `54f391b`）
- 今日五项工具管线工作全部合入：折叠策略随 actor 线程（`42e27e1`）、ToolResult 投影私有化（`4446264`）、todo_contract 对齐 v3（`1e15111`）、TimelineToolState 六态（`54f391b`）。
- 全仓测试零失败（`todo_contract` 存量失败已查明为测试过期并重写）。
- 已知遗留：`cargo fmt --all --check` 在 qaqh-config / qaqh-lsp 有**存量**格式问题（`0a41afa` 遗留，未处理）。

### qaqh-js-sdk（`205c8f2`）
- ⚠ **待同步**：内置 `src/protocol/bindings/` 是 2026-09-08 快照，缺两样：
  1. `TimelineToolState` 六态（缺 `cancelled` / `backgrounded`）
  2. `ControlCommand.session_attach`（TUI 的子代理观测已依赖它）
  同步方式：主仓 `cargo test -p qaqh-types --features ts` / `-p qaqh-domain --features ts` 再生成，对比 `crates/*/bindings/qaqh/` 与 SDK 的 `src/protocol/bindings/qaqh/`，手工同步（**根 `bindings/` 与 crate 级都可能是旧真相，以 Rust 源为准**，见 MEMORY.md「bindings 同步陷阱」）。

### qaqh-electron（`fcff0e5`）
- 初始导入完成，renderer 按六态 `TimelineToolState` 实现尚未开始；实现规格见仓内 `docs/implementation-spec.md` 与主仓 `docs/frontend-contract.md`（第 5 节有字段语义澄清表 + `docs/adr-toolstate-cancelled-backgrounded.md` 有前端跟进项）。

### qaqh-tui-app（`f85c33d`）
- 子代理实时观测（Ctrl+↑/↓，SessionAttach 只读接入）已提交推送，cargo check 干净。

## 5. 工作 tty 要点（详见入库的 `.workbuddy/memory/MEMORY.md`）

- 契约真源：主仓 `docs/frontend-contract.md`；前端变更走 RFC 流程。
- bindings 三层真相陷阱：crate 级再生输出 > 根 `bindings/` > SDK 快照；核对一律以 Rust 源 + crate 级再生为准。
- per-actor 状态一律 thread-local + `ActorToolScope` 搬运；测试判据勿用 `truncated` 布尔（24K 硬顶会置 true）。
- 全局状态测试走 `TEST_RUNTIME_SERIAL`；数据根用 `QAQH_DATA_DIR` 重定向。
- 提交：conventional commits + scope，breaking 带 `!` 与脚注。

## 6. 2026-09-09 当日 changelog

1. 修复折叠策略进程级全局污染（真 bug，双会话复现 → thread-local 修复）。
2. ToolResult 投影收敛：summary/model/output_ref 私有化 + 受控改写接口；外置路径语义澄清。
3. 诊断并修复 todo_contract 存量失败（测试过期，非注册缺失）。
4. TimelineToolState 补 cancelled/backgrounded：归档保真（canonical ToolResult）→ 投影全链打通；ADR + 契约文档 §5。
5. main 合并推送；两新仓 init + push；tui-app 未提交工作补交。
6. WorkBuddy 状态备份至 secret gist。
