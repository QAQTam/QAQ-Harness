[package]
name = "qaqh-proto"
version.workspace = true
edition.workspace = true
description = "QAQ-Harness shared data models and daemon discovery"

[dependencies]
qaqh-types = { path = "../qaqh-types" }

serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
