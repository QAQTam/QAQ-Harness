[package]
name = "qaqh-gate-testui"
version.workspace = true
edition.workspace = true
description = "Interactive web UI for testing the QAQ-Harness gate with a mock OpenAI API"

[dependencies]
qaqh-gate = { path = "../qaqh-gate" }
qaqh-types = { path = "../qaqh-types" }
serde_json = "1"
tiny_http = "0.12"

[lints.clippy]
unwrap_used = "deny"
string_slice = "allow"
