[package]
name = "qaqh-skills"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
qaqh-types = { path = "../qaqh-types", default-features = false }
serde = { version = "1", features = ["derive"] }
serde-saphyr = "1.1"

[features]

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
