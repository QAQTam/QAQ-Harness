# 2026-09-08

## Electron 前端可行性分析（codegraph + 3 子代理）

- 用 cargo metadata 提取了 16 crate 依赖图：types/config-api 为基底，runtime 是唯一聚合器（依赖其余 13 个），daemon 与 client 互不依赖、只挂协议层。`message→session` 仅为 dev-dep，无循环。
- bindings/ 的 71 个 .ts 由 ts-rs 从 domain/ringing/types 生成（`cargo build --features ts`），纯类型无运行时。
- 三个 Explore 子代理分别勘察：① TS bindings 覆盖面（缺错误码枚举/Config DTO/service 方法常量/content 上传类型）；② daemon HTTP 面（axum_impl/mod.rs:109 路由表、auth.rs 双头校验、debug_control.rs 的 /debug/ 托管 + __qaqh_bridge__ 注入、qaqh-client 公开 API）；③ 核心调用链（command.rs:152 → registry.rs:461 每会话 actor 线程 → engine_turn.rs:969 run_lap → hub.rs:490 fanout；timeline journal 为 transcript 权威）。
- 结论：Electron 前端完全可行（frontend-contract.md/webui 标准已背书，/debug/ 托管即 electron-vite 布局），设计为"只做协议对端"：main 管 daemon 生命周期，preload 桥传 endpoint+token，renderer 按 webui §6 SDK 分层。注意 winui3-sync-issue.md 决策 C 曾倾向 Tauri 而非 Electron。
- 产出：.workbuddy/electron-frontend-design.md（完整设计文档）+ 3 张 SVG 图（依赖分层/命令主链/Electron 架构）。

## qaqh-js-sdk 完成（23:15-23:55）

- Node v24.20.0 安装在 ~/.local/opt/node-v24.20.0-linux-x64（~/.local/bin 符号链接；每个 Bash 命令需 export PATH=$HOME/.local/bin:$PATH）。
- SDK 落地 /home/qaqtamsy/Projects/qaqh-js-sdk：14 个手写源文件 + 72 个 ts-rs bindings 拷贝；exports 双入口 ./ 与 ./node；tsc 零错误；examples/smoke.mjs 对真实 daemon 冒烟 12/12 通过（open/lease/服务面/session_create/attach/timeline/content 往返/SSE batch/close/stop-if-idle）。
- 真机验证回填的协议事实（文档未载）：
  1. 读 timeline/bootstrap 前必须先发 control `session_resume` 把 seed 附着到当前 lease（session_create 后也要），否则 401 lease_required "attach the session seed"。
  2. GET timeline 响应是包装页 {schema,version,server_epoch,seed,snapshot:{watermark,turns},has_more,total_turns?}（镜像 qaqh-client::TimelinePage），不是裸 TimelineSnapshot。
  3. session.list 返回富元数据对象（seed/created_at/model/effort/message_count/turn_count/archived/usage_totals…）。
  4. QAQH_DATA_DIR 重定向目录名必须叫 "qaqh"（platform.rs 安全校验），如 /tmp/x/qaqh。
  5. RingingCommandAckStatus 只有 accepted|rejected；dispatch_failed/timed_out 只以 HTTP 502/504 出现。
  6. ControlEvent 联合里有裸 number 占位变体，switch 前需 typeof 守卫。
  7. Node ESM 下 barrel 的 export * 必须带 .js 扩展名（bindings 内部 import type 编译后消失不受影响）。
- 版本约束：electron-vite 5 peer vite ^5||^6||^7（vite 8 未支持）→ 壳用 vite 7.3.6 + @vitejs/plugin-react 6.1.1 + electron 44 + tailwind 4.3 + zustand 5。
- Electron 壳（任务 #8）已按用户要求暂停，尚未写任何代码。
